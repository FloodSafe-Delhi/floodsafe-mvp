# FloodSafe ML Service - FHI-Only Deployment (Lightweight)
# This minimal requirements file enables FHI calculation without heavy ML dependencies
# Total size: ~50MB (vs 1.4GB+ for full requirements.txt)
#
# FHI uses Open-Meteo API (free, no auth) for weather data

# Web Framework
fastapi>=0.100.0
uvicorn[standard]>=0.23.0

# Data Validation
pydantic>=2.0.0
pydantic-settings>=2.0.0

# HTTP Client (for Open-Meteo API)
httpx>=0.24.0

# Utilities
python-dotenv>=1.0.0
numpy>=1.24.0
